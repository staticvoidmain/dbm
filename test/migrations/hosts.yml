hosts:
  dev:
    marketing:
      vehdor: postgres
      host: localhost
    sales:
      vendor: sqlite3
      host: c:/dev/projects/dbm/test/sqlite.db
  test:
    marketing:
      vendor: mssql
      host: localhost
  prod:
    marketing: 
      vendor: postgres
      host: localhost