hosts:
  local:
    dragon:
      vendor: mssql
      security: integrated
      host: CYCSQLAPPD03
  dev:
    marketing:
      vendor: postgres
      host: localhost
    sales:
      vendor: sqlite
      host: c:/dev/projects/dbm/test/data/sqlite.db
  test:
    marketing:
      vendor: mssql
      host: (localdb)\\spec
  prod:
    marketing: 
      vendor: mysql
      host: localhost