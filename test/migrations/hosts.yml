hosts:
  dev:
    marketing:
      vendor: postgres
      host: localhost
    sales:
      vendor: sqlite3
      host: c:/dev/projects/dbm/test/data/sqlite.db
  test:
    marketing:
      vendor: mssql
      host: (localdb)\\spec
  prod:
    marketing: 
      vendor: postgres
      host: localhost