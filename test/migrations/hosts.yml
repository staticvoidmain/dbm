---
hosts:
  sales:
    dev:
      user: pg_sql
      admin: postgres
      vendor: sqlite3
      host: c:/dev/projects/dbm/test/sqlite.db

    test:
      vendor: mssql
      host: localhost

    prod: 
      vendor: postgres
      host: localhost